#include <iostream>
#include <vector>

class matrix {};

bool compare(const matrix &m1, const matrix &m2);

std::vector<int>::iterator change_val(int n, std::vector<int>::iterator it);

int main()
{
    return 0;
}
